interface:
  display_name: "Waterbuddy App"
  short_description: "Repo-specific patterns and workflows for waterbuddy-app"
  default_prompt: "Use the waterbuddy-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true